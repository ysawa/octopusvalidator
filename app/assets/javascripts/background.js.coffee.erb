$ ->
  agent = navigator.userAgent
  # unless agent.search(/iPhone/) != -1 || agent.search(/iPad/) != -1 || agent.search(/iPod/) != -1 || agent.search(/Android/) != -1
  #   $.backstretch("<%= asset_path 'bg.jpg' %>")
